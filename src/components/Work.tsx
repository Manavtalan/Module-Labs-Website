import {ArrowUpRight} from 'lucide-react';
import {Label} from './Common';
import {projects,type Project} from '../data';
import ProjectVisual from './ProjectVisual';
export default function Work({onProject}:{onProject:(project:Project)=>void}){return <section id="work" className="section work-section"><div className="section-head reveal"><div><Label>SELECTED WORK</Label><h2>A few things we’ve<br/><em>put into the world.</em></h2></div><div className="section-aside"><p>Different disciplines.<br/>The same attention to detail.</p><span className="mono">A selection of studio concepts / 2026</span></div></div><div className="project-grid">{projects.map((p,i)=><button key={p.id} className={`project-card project-${i+1} ${p.size} reveal`} onClick={()=>onProject(p)} aria-label={`View ${p.name} — ${p.category} concept`}><div className="project-media"><ProjectVisual kind={p.kind}/><span className="view-project">View concept <ArrowUpRight size={15}/></span></div><div className="project-meta"><div><h3>{p.name}</h3><span>{p.category} <span className="meta-dot">·</span> Concept</span></div><ArrowUpRight size={20}/></div></button>)}</div><div className="work-end mono"><span>Ideas made tangible.</span><span>DESIGN / DEVELOPMENT / EXPERIMENTATION</span></div></section>}
