import {lazy,Suspense} from 'react';
import {motion,useReducedMotion} from 'framer-motion';
import {ArrowDown} from 'lucide-react';
import {CTA} from './Common';
const Scene=lazy(()=>import('./Scene'));
export default function Hero({onContact}:{onContact:()=>void}){const reduce=useReducedMotion();const enter=(delay:number)=>({initial:{opacity:reduce?1:0,y:reduce?0:22},animate:{opacity:1,y:0},transition:{duration:.85,delay,ease:[.16,1,.3,1] as [number,number,number,number]}});return <section className="hero" aria-labelledby="hero-title"><div className="hero-note mono"><span className="status-dot"/> Independent minds. Shared ambition.</div><motion.div className="hero-content" {...enter(.1)}><span className="hero-kicker mono">A DESIGN + DEVELOPMENT STUDIO</span><h1 id="hero-title"><motion.span {...enter(.2)}>We design and build</motion.span><motion.span {...enter(.35)}>websites where</motion.span><motion.em {...enter(.5)}>art meets technology<span className="orange">.</span></motion.em></h1><motion.p {...enter(.65)}>Creating distinctive digital experiences through thoughtful<br className="desktop-break"/> design, motion, 3D, and interaction.</motion.p><motion.div {...enter(.8)}><CTA onClick={onContact}/><div className="availability mono"><span className="status-dot"/> Available for select projects</div></motion.div></motion.div><div className="hero-object"><Suspense fallback={<div className="module-fallback">✳</div>}><Scene mode="hero"/></Suspense><span className="object-caption mono">ART × ENGINEERING</span></div><div className="hero-bottom mono"><span>Small studio. Expansive thinking.</span><a href="#work">Explore our work <ArrowDown size={15}/></a><span>Built with intention, everywhere.</span></div></section>}
