import {useState} from 'react';
import {Instagram,Mail,MessageCircle,Menu,X,ArrowUpRight} from 'lucide-react';
import {Wordmark} from './Common';
import {studio} from '../data';
const links=['Work','Services','Pricing','FAQs'];
export default function Navbar({onContact}:{onContact:()=>void}){const [open,setOpen]=useState(false);const whatsappUrl=studio.whatsapp?`https://wa.me/${studio.whatsapp.replace(/\D/g,'')}`:'';return <><a className="skip-link" href="#main">Skip to content</a><header className="nav-shell"><nav className="navbar" aria-label="Main navigation"><Wordmark/><div className="nav-links">{links.map(l=><a key={l} href={`#${l.toLowerCase()}`}>{l}</a>)}</div><div className="nav-actions"><a className="icon-button contact-icon" href={`mailto:${studio.email}`} aria-label="Email Module Labs"><Mail size={15}/></a>{whatsappUrl?<a className="icon-button contact-icon" href={whatsappUrl} target="_blank" rel="noreferrer" aria-label="Message Module Labs on WhatsApp"><MessageCircle size={15}/></a>:<button className="icon-button contact-icon" onClick={onContact} aria-label="Contact Module Labs on WhatsApp"><MessageCircle size={15}/></button>}{studio.instagram?<a className="icon-button contact-icon" href={studio.instagram} target="_blank" rel="noreferrer" aria-label="Visit Module Labs on Instagram"><Instagram size={15}/></a>:<button className="icon-button contact-icon" onClick={onContact} aria-label="Contact Module Labs — Instagram link coming soon"><Instagram size={15}/></button>}<button className="icon-button mobile-toggle" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>{open?<X size={19}/>:<Menu size={19}/>}</button></div></nav>{open&&<div id="mobile-menu" className="mobile-menu">{links.map(l=><a key={l} href={`#${l.toLowerCase()}`} onClick={()=>setOpen(false)}>{l}<ArrowUpRight size={18}/></a>)}</div>}</header></>}
