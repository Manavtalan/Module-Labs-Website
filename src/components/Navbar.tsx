import {useState} from 'react';
import {Mail,MessageCircle,Menu,X,ArrowUpRight} from 'lucide-react';
import {Wordmark} from './Common';
const links=['Work','Services','Pricing','FAQs'];
export default function Navbar({onContact}:{onContact:()=>void}){const [open,setOpen]=useState(false);return <><a className="skip-link" href="#main">Skip to content</a><header className="nav-shell"><nav className="navbar" aria-label="Main navigation"><Wordmark/><div className="nav-links">{links.map(l=><a key={l} href={`#${l.toLowerCase()}`}>{l}</a>)}</div><div className="nav-actions"><a className="icon-button" href="mailto:hello@modulelabs.design" aria-label="Email Module Labs"><Mail size={17}/></a><button className="icon-button whatsapp" onClick={onContact} aria-label="Contact Module Labs on WhatsApp"><MessageCircle size={17}/></button><button className="nav-cta" onClick={onContact}>Start a project <ArrowUpRight size={16}/></button><button className="icon-button mobile-toggle" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>{open?<X size={20}/>:<Menu size={20}/>}</button></div></nav>{open&&<div id="mobile-menu" className="mobile-menu">{links.map(l=><a key={l} href={`#${l.toLowerCase()}`} onClick={()=>setOpen(false)}>{l}<ArrowUpRight size={18}/></a>)}</div>}</header></>}
